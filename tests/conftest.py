"""
Содержит тестовые fixture для pytest, используемые во всех тестах проекта.
Обеспечивает единую точку настройки тестового окружения, включая
создание экземпляра приложения.

Основные возможности:
- Предоставляет fixture `app` для получения экземпляра Flask-приложения.
- Предоставляет fixture `client` для выполнения HTTP-запросов.
- Позволяет изолировать тесты от рабочей конфигурации.
- Обеспечивает единообразное окружение для всех тестовых сценариев.
"""

import pytest

from app import create_app


@pytest.fixture
def app():
    """
    Fixture для создания тестового экземпляра Flask-приложения.

    Создаёт и возвращает экземпляр приложения с тестовой конфигурацией
    через вызов `create_app()` из основного модуля приложения.

    Назначение:
    - Используется в тестах для получения доступного к тестированию
      экземпляра приложения.
    - Обеспечивает изолированную среду для каждого тестового сценария.
    - Позволяет имитировать запросы через тестовый клиент (`test_client`).

    Возвращаемое значение:
    Flask
        Настроенный экземпляр Flask-приложения с тестовой конфигурацией.

    Пример использования в тестах:
        def test_index_page(app):
            client = app.test_client()
            response = client.get('/')
            assert response.status_code == 200

    Примечания:
    - Каждый вызов fixture создаёт новый экземпляр приложения.
    - Конфигурация приложения определяется в `create_app()`
    """
    app = create_app()
    yield app


@pytest.fixture
def client():
    """
    Fixture для получения тестового клиента Flask.

    Создаёт и возвращает объект тестового клиента (`test_client`),
    позволяющий имитировать HTTP-запросы к приложению в тестовой среде.

    Назначение:
    - Используется в тестах для отправки HTTP-запросов (GET, POST и др.)
      к эндпоинтам приложения.
    - Обеспечивает изолированное взаимодействие с приложением
      без влияния на рабочую среду.
    - Позволяет проверять статусы ответов, заголовки и содержимое.

    Возвращаемое значение:
    FlaskClient
        Экземпляр тестового клиента Flask, готовый к выполнению запросов.

    Пример использования в тестах:
        def test_login_endpoint(client):
            response = client.post('/login', json={
                'username': 'testuser',
                'password': 'testpass'
            })
            assert response.status_code == 200
            assert response.json['success'] is True
    """
    from app import app
    with app.test_client() as client:
        yield client
