"""
Этот модуль — простое Flask-приложение, отображающее приветственное сообщение.

Основные компоненты:
- Flask для создания веб-приложения.
- Главный маршрут (/), возвращающий 'Ну, понеслась!'.
- Запуск при прямом выполнении файла.

Функционал:
- Главная страница показывает одно сообщение.
- Без сложной логики и настроек.
- Запускается локально для тестирования.

Пример использования:
1. Установите Flask: `pip install Flask`.
2. Запустите файл командой `python your_file.py`.
3. Откройте localhost:5000 в браузере.

Ограничения:
- Нет дополнительных маршрутов и обработчиков ошибок.
- Отсутствует защита от SQL-инъекций и XSS-атак.
- Один маршрут — не подходит для сложных приложений.
"""

from flask import Flask

app = Flask(__name__)


@app.route('/')
def main():
    return 'Ну, понеслась!'


if __name__ == '__main__':
    app.run()
